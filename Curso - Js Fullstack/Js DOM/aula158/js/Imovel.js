class Imovel {
    //construtor para construir a classe imovel com essas caracteristicas
    constructor(tipo, area, alugado) {
        //todos esses atributos receberão o que for passado como parametro
        this.tipo = tipo;
        this.area = area;
        this.alugado = alugado;
    }
}
